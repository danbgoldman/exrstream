# exrstream runtime deps. All install from aarch64 wheels; ffmpeg is not used.
# (aiortc pulls in PyAV, which bundles libav* -- as PyNvVideoCodec already does.
# Nothing shells out to an ffmpeg binary, and no video passes through either.)
aiohttp==3.14.3
aiortc==1.15.0
numpy==2.5.2
openimageio==3.1.17.0
opencolorio==2.5.2
pynvvideocodec==2.2.2
PyOpenGL==3.1.10

